import Section from '@/components/page/computer/Section';
import React from 'react';

const page = () => {
    return (
        <div>
            <Section/>
        </div>
    );
};

export default page;